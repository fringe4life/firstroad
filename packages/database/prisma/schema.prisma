generator client {
  provider        = "prisma-client"
  previewFeatures = ["relationJoins", "partialIndexes"]
  engineType      = "client"
  output          = "../generated/prisma"
  runtime         = "bun"
  compilerBuild   = "fast"
}

datasource db {
  provider = "postgresql"
}
