generator client {
  provider        = "prisma-client"
  previewFeatures = ["relationJoins"]
  engineType      = "client"
  output          = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
