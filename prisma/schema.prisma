generator client {
  provider        = "prisma-client"
  previewFeatures = ["relationJoins"]
  engineType      = "client"
  output          = "../src/generated/prisma"
  runtime         = "bun"
}

datasource db {
  provider = "postgresql"
}
