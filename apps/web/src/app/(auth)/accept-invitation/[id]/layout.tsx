const AcceptInvitationLayout = ({
  children,
}: LayoutProps<"/accept-invitation/[id]">) => (
  <div className="grid min-h-dvh w-full place-content-center">{children}</div>
);

export default AcceptInvitationLayout;
