const TicketEditLayout = ({ children }: { children: React.ReactNode }) => (
  <div className="grid h-full grid-rows-[min-content_1fr] gap-y-4">
    {children}
  </div>
);

export default TicketEditLayout;
