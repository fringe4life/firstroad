"use cache: remote";
import { Heading } from "@/components/heading";

const TicketsHeading = async () => (
  <Heading description="All your tickets at one place" title="My Tickets" />
);

export { TicketsHeading };
